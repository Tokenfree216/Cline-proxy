version: "3.9"

services:
  cline-proxy:
    build: .
    container_name: cline-proxy
    restart: unless-stopped
    ports:
      - "8083:3457"
    volumes:
      - ./data:/app/data
      - ./override.md:/app/override.md:ro
    environment:
      - PORT=3457
